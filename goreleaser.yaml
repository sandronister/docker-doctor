version: 2

project_name: dockdoc

builds:
  - main: ./cmd/dockdoc
    binary: dockdoc
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'
